name: Pipeline
on:
  push:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout node
        uses: actions/checkout@v4
      - name: Hello
        run: echo "Workflow runned successfully!"
      - name: List files
        run: ls -la
